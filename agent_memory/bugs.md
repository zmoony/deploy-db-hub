# 已知问题

- 已确认 Qt/CMake/Ninja/MinGW 位于 `D:\systemInsall\Qt`，Release 构建、单元测试和 windeployqt 打包可运行。
- Stub 远程实现已移除；Linux 使用 OpenSSH CLI，Windows 使用 winrs。后续可切换为 libssh/libcurl 原生实现。
- 2026-06-17 已修复：Windows 客户端通过 OpenSSH 访问 Linux 时，若启用 `ControlMaster/ControlPath/ControlPersist`，服务器监控和远程文件页可能报 `getsockname failed: Not a socket`；现 Windows 下禁用连接复用参数。
- 2026-06-18 已修复：服务状态 `pgrep -f` 会匹配 SSH 执行检测命令时的 shell 自身，导致显示不存在的 PID/假运行；已改为 bracket pattern + PID 存活校验。
- 2026-06-22 已修复：启动时部署页自动刷新远程日志列表与服务状态会多次弹密码/host key 确认框；根因是构造函数、下拉联动和选择器刷新使用了允许 prompt 的远程刷新路径。现启动/自动联动改为静默刷新, 只有用户点击「刷新列表」「刷新状态」、查看日志或部署等主动操作才允许弹框。
- 2026-06-23 已修复：启动时若服务器密码已保存, `refreshServiceStatus(false)` 仍会同步 SSH 探测并阻塞构造; 静默模式现仅显示「未检测」。主界面模块 Tab 原位于侧栏上方整行, 已移入右侧内容区以匹配参考图左右布局。
- 2026-06-23 已修复：启动时出现多个临时窗口/弹框；根因是通用工具、大数据、数据库页面从旧 `QStackedWidget` 转移到主窗口前调用了 `page->show()`，以及 `PageLayout::wrapContentPanel` 在页面拥有主窗口父级前提前显示页面，导致 Qt 将其短暂显示为顶层窗口。现移除这些提前 `show()`，由主窗口布局统一显示。
- 2026-06-24 已修复：Redis Key 管理/查看总是「读取 Redis 响应超时」；根因是 `RedisServiceClient` 在 `QtConcurrent` 后台线程使用 `QTcpSocket::waitForReadyRead()`，Windows 下无事件循环时该 API 不可靠。现改为 `QEventLoop` + `readyRead` 等待，并缓冲按字节读取 RESP bulk。
- 2026-06-24 已修复：Elasticsearch 索引表「状态」列 green/yellow/red 标签文字被裁切；根因是 `setMaximumHeight(24)` 与 QSS padding/border 及单元格上下边距叠加。现按字体度量计算标签尺寸，去掉垂直边距。
- 2026-06-24 已修复：全局设置页进入后顶部「通用工具/部署工具/…」模块 Tab 消失；设置改为与模块页并列切换，模块 Tab 始终保留。JDBC 检测仅校验用户选择的驱动 JAR（不再误报缺少 jdbc-sql-runner.jar）；打包脚本同步复制 `tools/jdbc-sql-runner.jar`。
- 2026-06-24 已修复：Kafka 主题「查看」报「远程命令无执行权限」；根因是 `consumeLatest` 使用 bash 进程替换 `< <(...)` 且仅调用 `kafka-get-offsets.sh`，与已成功加载主题统计的 `offsetRunnerFunction`/Admin Bridge 路径不一致。现优先走 `kafka-admin-runner` 的 `consume-latest` action，shell 回退改为临时文件 + `offsetRunnerFunction`。
- 2026-06-25 已修复：Kafka 主题管理卡顿；根因有二：① `KafkaAdminBridge::ensureRunnerUploaded` 每次调用都 SCP 上传 jar（打开主题页触发 2 次）；② shell 回退路径（`KAFKA_HOME` 未设置时）串行启动 7 次 JVM（describe + 5 offsets + consumer-groups），每次 2-3s。修复：① 上传前用 `wc -c` 检查远端 jar 大小一致则跳过；② shell 回退跳过 today/yesterday/week 时间维度 offset 查询（7→4 JVM），时间维度列无数据时显示 "-"。Admin Bridge 可用时仍走单 JVM 全量 dashboard。
- 2026-06-25 已修复：Kafka 主题页统计列（消费者组数/数据总量）不显示；根因：shell 脚本中 `kafka-consumer-groups.sh` 是最后一个命令，若它失败（超时/权限），整个脚本退出码非 0，`runRemoteKafka` 返回失败，触发 `dashboard.latest.isEmpty()` 检查使整个 stats 路径失败。修复：① 脚本每个命令加 `|| true` + 末尾 `exit 0`，部分失败不阻塞整体；② `loadTopicDashboard` 接受 `knownTopics` 参数，stats 路径复用 quick 路径的 describe 结果，跳过重复 describe（5→4 JVM）；③ latest/earliest 为空时不返回失败，total 显示 "-"；consumer-groups 为空时 groups 显示 "-"。
- 2026-06-25 已修复：一键部署 service-command 模式重启报 `Unable to access jarfile xxx.jar`；根因：`RemoteExecutor::execute` 不支持 workingDirectory，SSH 会话 cwd 为登录用户家目录，用户写的 `java -jar xxx.jar` 相对路径在 cwd 下找不到 jar。修复：新增 `wrapCommandWithWorkingDirectory`（Linux `cd '<dir>' && <cmd>` / Windows `cd /d "<dir>" && <cmd>`），`executeProjectRestart` 在非脚本上传模式下自动用产物所在目录包装命令。脚本模式脚本内部已 cd，不重复包装。新增 3 个 ProjectServiceConfigTest 用例；Release 构建与 deploy_hub_tests 通过。
- 2026-06-25 已修复：AI 辅助/聊天流式输出体感慢。根因：每个 SSE delta（通常 1 token/字）到达时都做 `moveCursor(End)→insertPlainText→moveCursor(End)`，QPlainTextEdit 每次都触发布局/重绘，UI 线程被海量小事件淹没，导致"输出慢"的体感（实际网络响应正常）。修复：新增 [AiStreamBuffer.h/.cpp](file:///d:/project/code/vibe-coding/deploy-hub/src/ui/AiStreamBuffer.h) 工具类：把 delta 追加到内存缓冲，用 30ms 单次 QTimer 做 coalescing，flush 时一次性 `QTextCursor::insertText` + `ensureCursorVisible`，把 UI 刷新频率从「N tokens/秒」降到 ~30Hz。[AiChatWidget](file:///d:/project/code/vibe-coding/deploy-hub/src/ui/AiChatWidget.cpp)、[LogAiAnalysisWidget](file:///d:/project/code/vibe-coding/deploy-hub/src/ui/LogAiAnalysisWidget.cpp)、[CommonToolsWidget::wireAiAssist](file:///d:/project/code/vibe-coding/deploy-hub/src/ui/CommonToolsWidget.cpp#L1938) 三处 deltaReceived 统一接入 AiStreamBuffer；finished/failed/abort/clear 路径都加 flush()/reset() 确保不丢尾字、不残留缓冲。Release 构建 + ctest 1/1 + 冒烟 started-ok + 重新打包 dist 成功。
- 2026-06-30 已修复：Kafka 主题查看/列表再次变慢；根因：① Admin Bridge 可用时主题页仍走 quick+dashboard 两阶段，重复启动 2 次 JVM 且各做一次全量 describe；② shell 回退 `consumeLatest` 用全集群 `offsets -1` 再过滤单主题，主题多时极慢。修复：① Bridge 可用时主题页改为单次 `loadKafkaTopicStats`；② `consumeLatest` shell 回退改为 `--topic` 限定 offset 查询；③ Admin Runner `consume-latest` 预览分区 8→2、poll 2500→1500ms。
